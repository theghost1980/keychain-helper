# Directorios generados por npm/yarn/pnpm
node_modules/

# Directorios de build/compilación
dist/
build/
out/

# Logs de npm
npm-debug.log*
yarn-debug.log*
yarn-error.log*
lerna-debug.log*

# Archivos de sistema específicos del SO
.DS_Store # macOS
Thumbs.db # Windows

# Archivos temporales o de configuración local
.env # Archivos de entorno local (si los usas)
.env.local # Archivos de entorno local específicos

# IDE-specific files
# .vscode/
# Depende si quieres compartir la configuración de VS Code con otros colaboradores.
# Generalmente, .vscode/settings.json y .vscode/launch.json se suelen incluir,
# pero podrías querer ignorar otros archivos temporales del IDE.
# Por ahora, mantendremos .vscode/ fuera del gitignore por defecto,
# asumiendo que compartirás settings.json.

# Archivos de paquetes (npm pack)
*.tgz
*.gz

# Archivos de pruebas o coverage
coverage/
.nyc_output/

# Archivos generados por herramientas de linting/formatting
.eslintcache
.prettiercache